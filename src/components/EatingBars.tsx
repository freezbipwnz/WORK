/**
 * Оверлей «кушает 🍽» ОТКЛЮЧЁН: индикатор трапезы теперь — часть баббла над
 * головой клиента (EatBubble в Scene.tsx, ~68px выше опорной точки, над
 * посадочной точкой для сидящего). Одна точка рендера — без двух индикаторов
 * одновременно. Компонент оставлен как no-op, т.к. смонтирован в GameScreen.
 */
export default function EatingBars() {
  return null
}
